# YouTube AI Comment Guard

YouTubeクリエイターのメンタルヘルスを守るための、コメント自動管理・要約システム。

## 機能

- 🛡️ **フィルタリング**: 攻撃的なコメントを自動で非表示/保留
- 🔄 **解毒変換**: 批判的コメントを事務的な報告形式に変換
- 📊 **ダッシュボード**: ポジティブな意見と改善提案のみを表示

## 技術スタック

- **Backend**: Python (FastAPI) + uv on Google Cloud Run
- **Frontend**: React (Vite) on AWS CloudFront + S3
- **AI**: Vertex AI (Gemini 1.5 Flash)
- **Database**: Firestore
- **Infrastructure**: Terraform (GCP + AWS)

## アーキテクチャ

```
┌─────────────────────────────────────────────────────────────────┐
│                         AWS                                      │
│  ┌─────────────┐    ┌──────────────┐    ┌─────────────┐        │
│  │  Route 53   │───▶│  CloudFront  │───▶│    S3       │        │
│  │             │    │              │    │ (Frontend)  │        │
│  └─────────────┘    └──────────────┘    └─────────────┘        │
└─────────────────────────────────────────────────────────────────┘
                              │ API Requests
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        GCP                                       │
│  ┌─────────────┐    ┌──────────────┐    ┌─────────────┐        │
│  │  Cloud Run  │───▶│  Vertex AI   │    │  Firestore  │        │
│  │  (Backend)  │    │  (Gemini)    │    │             │        │
│  └─────────────┘    └──────────────┘    └─────────────┘        │
└─────────────────────────────────────────────────────────────────┘
```

## セットアップ

### 開発環境

```bash
# 依存関係インストール
make install

# バックエンド起動
make dev-backend

# フロントエンド起動 (別ターミナル)
make dev-frontend
```

### デプロイ

#### インフラ構築 (Terraform)

```bash
# Terraformセットアップ
make terraform-setup
make terraform-init

# ワークスペース作成
make terraform-workspace-new ENV=dev

# デプロイ
make terraform-plan ENV=dev
make terraform-apply ENV=dev
```

#### フロントエンドデプロイ (AWS S3 + CloudFront)

```bash
# Dev環境へデプロイ
make deploy-frontend-dev

# Prd環境へデプロイ
make deploy-frontend-prd
```

## 環境変数

### Backend (GCP)
- `GOOGLE_CLOUD_PROJECT`: GCPプロジェクトID
- `ENVIRONMENT`: 環境 (`dev` or `prd`)
- `FRONTEND_URL`: フロントエンドURL (CORS設定用)

### Frontend
- `VITE_API_URL`: バックエンドAPIのURL

### YouTube OAuth
- `YOUTUBE_CLIENT_ID`: YouTube OAuth Client ID
- `YOUTUBE_CLIENT_SECRET`: YouTube OAuth Client Secret

## ドメイン

| 環境 | フロントエンド | バックエンド |
|------|----------------|--------------|
| dev  | youtube-comment-guard.dev.devtools.site | Cloud Run URL |
| prd  | youtube-comment-guard.devtools.site | Cloud Run URL |
